Put the exported model here as `model.glb` (i.e. `public/model.glb`).

Export it from mc-studio-frontend's browser console while a closet is loaded:

```js
window.downloadGLB('model.glb')
```

Then move the downloaded file into this folder.
